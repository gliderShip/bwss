{% form_theme form 'bootstrap_3_layout.html.twig' %}

    <h4>Please pick a Service category:</h4>
    {{ form_start(form) }}
    {% set url = path('offer_categories') %}
    {{ form_row(form.category, {'attr': {"ic-post-to": url, "ic-target":"#replace" } }) }}
    {{ form_row(form.services) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}